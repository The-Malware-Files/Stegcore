# Stegcore CLI usage

This page moved. The CLI reference now lives in one place:

- **[docs/cli-reference.md](docs/cli-reference.md)**: every command, every flag,
  every JSON reply shape, the configuration file and the exit codes.

If you're new, [docs/user-guide.md](docs/user-guide.md) is the friendlier way
in, and `stegcore wizard` walks you through a first embed with no flags at all.

This file is kept so older links still land somewhere useful.
